

defmodule Test.ApiKeyTest do
  use ExUnit.Case
  use PlugHelper

  # test "can create an apikey" do
  #   {conn, req_body} = simulate_json(Vulnpub.Router, :post, "api/v1/apikey", "test/json/new_apikey.json")
  #   {:ok, resp_body} = JSON.decode(conn.resp_body)
  # end

  # test "can destroy an apikey" do
  #   conn = simulate_request(Vulnpub.Router, :delete, "api/v1/apikey", "test/json/del_apikey.json")
  #   {:ok, resp_body} = JSON.decode(conn.resp_body)
  # end


end
